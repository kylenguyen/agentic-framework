#!/bin/sh
# osascript stub for `clipboard info`: /tmp/clipboard.kind says png or text.
if [ "$(cat /tmp/clipboard.kind 2>/dev/null)" = png ]; then echo '«class PNGf», 95, «class 8BPS», 1234'; else echo '«class utf8», 5, string, 5'; fi
